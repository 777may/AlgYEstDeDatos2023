ISTA previo = L, ultimo=L->siguiente;
        // while (ultimo->siguiente!=NULL)
        // {
        //     previo = ultimo;
        //     ultimo = previo->siguiente;
        // }
        // free(ultimo);
        // p